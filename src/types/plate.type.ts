export type PlaceType = {
    place?: string
    country_code?: string
    type?: string
    description?: string
    location?: [number, number]
    userLocation?: [number, number]
    userImage?: string
    userEmail?: string
    addLocation?: boolean
}
